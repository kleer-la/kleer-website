<div class="container-fluid fondo-celeste slide-header-generico">
	<div class="container">
		<div class="container-fluid container-header-generico">
			<div class="col-md-7">
				<h1><%= @category.name %></h1>
			</div>
			<div class="col-md-5 hidden-xs hidden-sm">
				&nbsp;<br/>
				<img src="/img/personajes/your-way-kleerer.png" height="400">
			</div>
		</div>
	</div>
</div>

<div class="container-fluid fondo-amarillo slide-contenedor-generico">
	<div class="container fondo-blanco contenedor-generico">
		<div class="col-md-9 contenedor-main-izq">
		
			<p class="lead"><i><%= @category.tagline %></i></p>
			<p><%= @markdown_renderer.render( @category.description )%></p>

			<h3><%=t("category.training")%></h3>
			<p>Además del acompañamiento y coaching personalizado, podemos hacer intervenciones más puntuales, realizando en tu organización, alguno de los siguientes entrenamientos privados.</p>

			<div class="panel-group" id="accordion">
				
				<% @event_types.each do |event_type| %>
				
					<% if event_type.elevator_pitch != "" %>
				
				  		<div class="panel panel-default">
						    <div class="panel-heading">
						    	<h4 class="panel-title">
							        <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion" href="#collapse<%= event_type.id %>">
							          <i class="icon-plus"></i>&nbsp;<%= event_type.name %></a>
							          <% if event_type.surveyed_count > 20 && !event_type.average_rating.nil? %>
				                        &nbsp;<div class="stars" id="rating<%= event_type.id %>" data-toggle="tooltip" data-placement="top" title="Rating: <%=event_type.average_rating%>/5 en base a <%=event_type.surveyed_count%> opiniones">
				                          <div style="width:<%=event_type.average_rating.to_f*16%>px;"/></div>
				                        </div>
				                        <script type="text/javascript">
				                        $(document).ready(function(){
				                            $('#rating<%= event_type.id %>').tooltip();
				                          });
				                        </script>
				                      <% end %>
							    </h4>
						    </div>
						    <div id="collapse<%= event_type.id %>" class="panel-collapse collapse">
						      <div class="panel-body">
						        	<%= event_type.elevator_pitch %>
								<br/>
								<a href="/<%=session[:locale]%>/categoria/<%=@category.codename%>/cursos/<%= event_type.uri_path%>"><%=t("category.see_more")%> >></a>
						      </div>
						    </div>
						  </div>
					
					<% end %>
					
				<% end %>
			</div>
			&nbsp;<br/>
			&nbsp;<br/>
			&nbsp;<br/>

		</div>

		<div class="col-md-3">
			<%= erb :_twitter_widget, :layout => false %>
		</div>
	</div>	
</div>
